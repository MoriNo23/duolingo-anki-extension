# Dependencias del Proyecto

## 📦 package.json

### Dependencies (Producción)
```json
{
  "@google/genai": "^1.34.0"
}
```

**@google/genai**: SDK oficial de Google para interactuar con la API de Gemini IA. Usado para:
- Generar mazos de Anki a partir de errores
- Categorizar errores automáticamente
- Proporcionar consejos personalizados

### DevDependencies (Desarrollo)
```json
{
  "typescript": "^5.9.3",
  "wxt": "^0.20.6"
}
```

**typescript**: Compilador de TypeScript para tipado estático y mejor desarrollo.

**wxt**: Framework moderno para desarrollo de extensiones de navegador. Proporciona:
- Build system optimizado
- Soporte para Manifest V3
- Desarrollo en caliente (hot reload)
- Soporte multi-navegador (Chrome, Firefox, Edge, Safari)

## 🚀 Scripts Disponibles

```json
{
  "dev": "wxt",                    // Desarrollo general
  "dev:firefox": "wxt -b firefox", // Desarrollo específico Firefox
  "build": "wxt build",            // Build para producción
  "build:firefox": "wxt build -b firefox", // Build Firefox
  "zip": "wxt zip",                // Empaquetar extensión
  "zip:firefox": "wxt zip -b firefox",    // Zip Firefox
  "compile": "tsc --noEmit",      // Verificación de tipos
  "postinstall": "wxt prepare"    // Configuración post-instalación
}
```

## 🔧 Configuración del Proyecto

- **Type**: Módulo ES (ESM)
- **Private**: true (proyecto privado)
- **Version**: 0.0.0 (versión inicial)

## 📋 Notas Importantes

1. **Mínimo de dependencias**: Solo lo esencial para mantener el proyecto ligero
2. **WXT Framework**: Elección moderna que simplifica el desarrollo de extensiones
3. **TypeScript**: Tipado estático para mayor robustez y mantenibilidad
4. **Google GenAI**: Única dependencia externa para funcionalidad de IA

## 🎯 Arquitectura de Dependencias

```
Extension Firefox (Manifest V3)
    ↓
WXT Framework (build system)
    ↓
TypeScript (tipado)
    ↓
@google/genai (IA)
    ↓
Duolingo + Anki (integración)
```

## 📊 Resumen

- **2 dependencias totales** (muy ligero)
- **1 dependencia de producción** (IA)
- **2 dependencias de desarrollo** (herramientas)
- **Framework moderno** (WXT)
- **Tipado estático** (TypeScript)
